# This heading should remain on one line even when it grows past the usual wrapping width for paragraphs.

This formatter should wrap ordinary paragraphs at eighty columns while keeping
the markdown content readable and stable for review.

> This blockquote should stay exactly as written even if it is longer than
> eighty columns because quoted text is out of scope.

- This list item should also wrap once it grows beyond the configured width so
  that continuation lines remain easy to scan.

```text
This code block should stay exactly as written even if it is much longer than eighty columns and contains spaces.
```
